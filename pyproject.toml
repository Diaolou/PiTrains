[project]
name = "pitrains"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "blinkt>=0.1.2 ; sys_platform == 'linux'",
    "nre-darwin-py>=0.4.1",
    "rpi-gpio==0.7.1 ; sys_platform == 'linux'",
]
